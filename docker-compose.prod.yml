services:
  core-service:
    image: ${DOCKERHUB_USERNAME}/ott-core-service:latest
    container_name: core-service
    restart: always
    env_file: .env.prod
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/ott_education_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD}
      MONGO_URI: mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}/${MONGO_DB_NAME}?retryWrites=true&w=majority
      CHAT_SERVICE_BASE_URL: http://chat-service:3001/api
      JAVA_OPTS: "-XX:+UseSerialGC -XX:TieredStopAtLevel=1 -Dspring.main.lazy-initialization=true -Xms64m -Xmx180m -XX:ActiveProcessorCount=1"
    expose:
      - "8080"
    deploy:
      resources:
        limits:
          memory: 300M 
    networks:
      - app-network

  assignment-service:
    image: ${DOCKERHUB_USERNAME}/ott-assignment-service:latest
    container_name: assignment-service
    restart: always
    env_file: .env.prod
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/ott_assignment_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD}
      JAVA_OPTS: "-XX:+UseSerialGC -XX:TieredStopAtLevel=1 -Dspring.main.lazy-initialization=true -Xms64m -Xmx180m -XX:ActiveProcessorCount=1"
    expose:
      - "8080"
    deploy:
      resources:
        limits:
          memory: 300M  
    networks:
      - app-network

  chat-service:
    image: ${DOCKERHUB_USERNAME}/ott-chat-service:latest
    container_name: chat-service
    restart: always
    env_file: .env.prod
    environment:
      NODE_ENV: production
      MONGO_CHAT_URI: mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}/${MONGO_DB_NAME}?retryWrites=true&w=majority
      MEDIASOUP_LISTEN_IP: 0.0.0.0
      MEDIASOUP_ANNOUNCED_IP: ${EC2_PUBLIC_IP}
      MEDIASOUP_RTC_MIN_PORT: 40000
      MEDIASOUP_RTC_MAX_PORT: 40050
    expose:
      - "3001"
    ports:
      - "40000-40050:40000-40050/udp"
      - "40000-40050:40000-40050/tcp"
    deploy:
      resources:
        limits:
          memory: 150M # Node.js memory footprint limit
    networks:
      - app-network

  gateway:
    image: nginx:alpine
    container_name: gateway
    restart: always
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf
      - ./gateway/certs:/etc/nginx/certs:ro
    ports:
      - "80:80"
      - "443:443"
    deploy:
      resources:
        limits:
          memory: 80M 
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
